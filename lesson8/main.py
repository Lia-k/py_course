import re

# text = "James like apples. Marta like lemons."

# if (result := re.findall("[A-Z][a-z]+", text)) is not None:
#     for name in result:
#         print(name)
#         print(len(result))

# if (result := re.match("[A-Z][a-z]+]", text)) is not None:
#     print("Hello")
# print(result)

numbers = [1, 2, 3, 4, 5]
# print(
#     list(
#         map(
#             lambda number: (res := number + 1) ** res,
#             numbers
#         )
#     )
# )
# print([len(numbers) for item in numbers if len(numbers)])
# print([res for item in numbers if (res := len(numbers))])
